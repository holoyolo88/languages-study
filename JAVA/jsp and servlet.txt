# JSP와 서블릿의 관계

jsp가 호출되면 컨테이너는 서블릿이 실행되었는지 확인하고, 실행되어 있지 않으면 컨테이너 소스 관리 폴더에서 클래스 파일이 있는지 확인함.
클래스 파일이 있다면 해당 서블릿을 실행하고 사용자 요청을 처리하지만, 없을 경우 .java 파일을 생성하고 컴파일한 후 .class 파일 생성.
클래스를 생성한 후에 해당 클래스는 서블릿으로 실행됨. 